---
const bgImage = 'url("/images/current-class.png")';
const progress = 32;
---

<section class="sections current-lesson" style={`--bg-image: ${bgImage}`}>
  <article class="lesson-card">
    <div class="content">
      <h2 class="dashboard-box-title">Curso de React</h2>
      <p>clase 2 - Introducción a JSX</p>
    </div>
    <footer class="footer">
      <label class="progress-label" for="file"
        >Progreso de la clase: <span>{progress}%</span></label
      >
      <progress class="lesson-progress" id="file" value={progress} max="100">
        <span>{progress}%</span>
      </progress>
    </footer>
  </article>
</section>

<style>
  .current-lesson {
    padding: 5rem;
    width: 100%;
    max-width: 1300px;
    background: linear-gradient(134deg, rgb(17, 1, 39) 50%, rgb(0 0 0 / 0%)),
      var(--bg-image);
    background-size: cover;
    background-position: center;
    min-height: 30dvh;
    border-radius: 12px;
    border: 1px solid var(--accent-color);
  }
  .lesson-card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 100%;
    height: 100%;
    border-radius: 0.5rem;
    color: white;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
  .content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .progress-label span {
    color: var(--accent-color);
  }

  progress {
    width: 100%;
    margin-top: 1rem;
    appearance: none;
    border-radius: 20px;
    color: var(--accent-color) !important;
  }
  progress::-webkit-progress-bar {
    border-radius: 20px;
  }
  progress::-webkit-progress-value {
    background-color: var(--accent-color);
  }
  progress::-moz-progress-bar {
    background-color: var(--accent-color);
  }


  progress::-webkit-progress-bar {
    background-color: <bg>;
    border-radius: 20px;
  }
  progress::-webkit-progress-value {
    background-color: <accent>;
    border-radius: 20px;
</style>

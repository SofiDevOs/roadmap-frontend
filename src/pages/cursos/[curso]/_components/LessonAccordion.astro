---
interface Props {
  sectionTitle: string;
  lessonList: Array<{ title: string; slug: string }>;
}
const params = Astro.params;
const { sectionTitle, lessonList } = Astro.props;
const isOpen = lessonList.some((lesson) => lesson.slug === params.leccion);
---

<details open={isOpen} class="acordion">
  <summary class="acordion-sumary">{sectionTitle}</summary>
  <ul class="acordion-list">
    {
      lessonList.map((lesson) => (
        <li
          class={`acordion-item ${params.lesson === lesson.slug ? "is-active" : ""}`}
        >
          <a class="acordion-link" href={lesson.slug}>
            {lesson.title}
          </a>
        </li>
      ))
    }
  </ul>
</details>

<style>
  .acordion {
    font-size: 1.8rem;
    color: var(--accent-color);
    border: 0.5px solid rgb(128 128 128 / 11%);
    padding: 2rem;
    cursor: pointer;
  }

  .acordion-item {
    padding: 1rem;
  }
  .acordion-item:hover {
    background: var(--secondary-color);
  }
</style>
